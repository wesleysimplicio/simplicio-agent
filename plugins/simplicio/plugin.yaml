name: simplicio
version: "0.3.0"
description: "Route managed-repo mutations through the Simplicio runtime, independently verify deterministic edits, and inject adaptive neural skill handles without loading the full catalog."
author: "NousResearch / WesleySimplicio"
hooks:
  - pre_llm_call
  - pre_tool_call
  - post_tool_call
  - transform_terminal_output
