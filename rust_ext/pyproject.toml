[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "hermes-fast"
requires-python = ">=3.11"
version = "0.1.0"

[tool.maturin]
module-name = "hermes_fast"
features = ["pyo3/extension-module"]
