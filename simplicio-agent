#!/usr/bin/env python3
"""
Simplicio Agent CLI launcher.

This wrapper should behave like the installed `simplicio-agent` command
(the `hermes` alias is deprecated compatibility), including subcommands
such as `gateway`, `cron`, and `doctor`.
"""

if __name__ == "__main__":
    from hermes_cli.main import main
    main()
